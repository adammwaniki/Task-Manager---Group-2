import NavBar from "../components/NavBar";

export default function About() {
    return (
      <>
        <header>
        <NavBar />
        </header>
        <main>

          {/*Add some code into this section for the body of the About component in the main tag*/}

        </main>
      </>
    );
  };